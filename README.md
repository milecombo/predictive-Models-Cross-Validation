# predictive-Models-Cross-Validation
Interpretable prediction for correlated variables + classification with resampling. Within Repeated stratified 10-fold CrossValidation: Train Partial Least Square model > latent scores > threshold classes with LDA >Predict held-out data > Compute performance + Variable Importance> aggregate over runs. P-values by permutations
